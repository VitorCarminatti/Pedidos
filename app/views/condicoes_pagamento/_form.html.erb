<%= simple_form_for(@condicao_pagamento) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
    <%= link_to t('link_to.back'), condicoes_pagamento_path, class: 'btn btn-primary' %>
  </div>
<% end %>
