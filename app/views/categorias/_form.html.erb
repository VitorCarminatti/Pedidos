<%= simple_form_for(@categoria) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
    <%= link_to t('link_to.back'), categorias_path, class: "btn btn-primary" %>
  </div>
<% end %>
